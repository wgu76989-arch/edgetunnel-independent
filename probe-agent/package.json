{
  "name": "edgetunnel-probe-agent",
  "version": "1.0.0",
  "private": true,
  "description": "Authenticated ARM probe relay for the edgetunnel Worker",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "check": "node --check server.js",
    "test": "node --test"
  },
  "engines": {
    "node": ">=18"
  }
}
